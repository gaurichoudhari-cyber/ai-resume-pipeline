name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Setup Node
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      # 3. Install dependencies
      - name: Install dependencies
        run: npm install

      # 4. Run ESLint (safe for ESLint v8)
      - name: Run ESLint
        run: npx eslint . || true

      # 5. Run tests (if any)
      - name: Run tests
        run: npm test --if-present

      # 6. Build project (React/Node safe)
      - name: Build project
        run: npm run build --if-present